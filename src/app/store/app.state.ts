import {
  generalInterface,
  ubicacionInterface,
  fisicoInterface,
  predioInterface,
} from "../screens/generar-visita/model/generar-visita.model";

export interface AppState {
  readonly general: generalInterface[];
}